# Agentic Control Plane App

This folder is archived.

The active frontend is TypeScript React in `src/AgenticControlPlane.Web`.
The active backend is C# .NET in `src/AgenticControlPlane.Api`.

Use `src/README.md` for run instructions.
